# System Modules

Core system-level configurations and optimizations.

## Modules

- **disable-suspend.nix** - Disables suspend/hibernate and replaces with shutdown
- **gc-optimization.nix** - Automatic garbage collection and store optimization

## Usage

These modules are imported in `configuration.nix` and provide system-level functionality that affects the entire NixOS system.