# Hardware Modules

Hardware-specific configurations and drivers.

## Modules

- **nvidia.nix** - Automatic NVIDIA GPU detection and driver configuration

## Usage

These modules are imported in `flake.nix` and provide hardware-specific functionality that is automatically enabled based on detected hardware.